# Map assets

Place top-down map backgrounds here as optimized WebP files. The current board expects `tokyo-2099.webp`, `wakanda.webp`, and `klyntar.webp`.

Missing files automatically fall back to the tactical terrain placeholders already visible in the app.